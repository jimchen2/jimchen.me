# Postgres connection (Neon). Leave empty to serve the local markdown
# posts in content/posts/ instead (likes/views/comments turn themselves off).
POSTGRESQL_URL=

# Public origin used for canonical URLs, RSS and og: tags.
NEXT_PUBLIC_SITE=http://localhost:3000

# Secret used to sign the like/view anti-abuse tokens.
likes_SECRET_KEY=
